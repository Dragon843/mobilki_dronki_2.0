using UnityEngine;
using UnityEngine.UI;

public class RightSliderLogic : MonoBehaviour
{
    /*public Slider thrustForceSlider;

    float yTransform;

    // Update is called once per frame
    void Update()
    {
        if(thrustForceSlider.value <= 0.2f && thrustForceSlider.value >= -0.2)
        {
            yTransform = 0f;
        }
        else if(thrustForceSlider.value <= 0.4f && thrustForceSlider.value > 0.2f)
        {
            yTransform = 0.25f;
        }
        else if(thrustForceSlider.value <= 0.6f && thrustForceSlider.value > 0.4f)
        {
            yTransform = 0.5f;
        }
        else if(thrustForceSlider.value <= 0.8f && thrustForceSlider.value > 0.6f)
        {
            yTransform = 0.75f;
        }
        else if(thrustForceSlider.value <= 1f && thrustForceSlider.value > 0.8f)
        {
            yTransform = 1f;
        }
        else if(thrustForceSlider.value >= -0.4f && thrustForceSlider.value < -0.2f)
        {
            yTransform = -0.25f;
        }
        else if(thrustForceSlider.value >= -0.6f && thrustForceSlider.value < -0.4f)
        {
            yTransform = -0.5f;
        }
        else if(thrustForceSlider.value >= -0.8 && thrustForceSlider.value < -0.6f)
        {
            yTransform = -0.75f;
        }
        else if(thrustForceSlider.value >= -1f && thrustForceSlider.value < -0.8f)
        {
            yTransform = -1f;
        }
    }*/
}
